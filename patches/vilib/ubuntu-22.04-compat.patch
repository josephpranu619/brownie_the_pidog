diff --git a/install.py b/install.py
index 9002cf2..828215b 100644
--- a/install.py
+++ b/install.py
@@ -105,7 +105,7 @@ def check_rpi_model():
 
 def check_raspbain_version():
     _, result = run_command("cat /etc/debian_version|awk -F. '{print $1}'")
-    return int(result.strip())
+    return 12
 
 def check_python_version():
     import sys
diff --git a/vilib/utils.py b/vilib/utils.py
index 560cc2f..d3bd25a 100644
--- a/vilib/utils.py
+++ b/vilib/utils.py
@@ -11,15 +11,15 @@ def run_command(cmd):
     return status, result
 
 def getIP():
-    wlan0 = os.popen("ifconfig wlan0 |awk '/inet/'|awk 'NR==1 {print $2}'").readline().strip('\n')
-    eth0 = os.popen("ifconfig eth0 |awk '/inet/'|awk 'NR==1 {print $2}'").readline().strip('\n')
+    def ipv4(interface):
+        line = os.popen(
+            f"ip -4 -o addr show dev {interface} scope global"
+        ).readline().strip()
+        if not line:
+            return None
+        return line.split()[3].split('/')[0]
 
-    if wlan0 == '':
-        wlan0 = None
-    if eth0 == '':
-        eth0 = None
-
-    return wlan0,eth0
+    return ipv4("wlan0"), ipv4("eth0")
 
 def check_machine_type():
     import platform
